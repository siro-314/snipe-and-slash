# SNIPE & SLASH リファクタリング計画書

**作成日**: 2026-02-10  
**実施者**: Claude  
**方針**: 慎重に、焦らず、完璧に

---

## 🎯 目標

### 達成すべき品質基準
- ✅ **疎結合**: ドメインロジック・UI・状態管理の完全分離
- ✅ **堅牢性**: 型安全性・エラーハンドリング強化
- ✅ **拡張性**: 新武器・新敵の追加が容易
- ✅ **可読性**: 1ファイル300行以内、明確な責務分離
- ✅ **テスタビリティ**: Pure関数中心、依存性注入

### 守るべき原則
- 🔒 **既存の動作を100%保持**（ゲームバランス・調整を維持）
- 🔒 **段階的移行**（各フェーズで動作確認）
- 🔒 **Git履歴の保全**（コミット粒度を細かく）

---

## 📊 現状分析

### 既存コード構造（game.js: 1249行）

```
1. GameState (グローバル)           ← Phase 2でクラス化
2. ModelManager (Module Pattern)    ← Phase 2でクラス化
3. updateHUD() (関数)              ← Phase 3でUIManagerに
4. sword (A-Frameコンポーネント)    ← Phase 4でクラス分離
5. projectile (コンポーネント)       ← Phase 4で分離
6. enemy-mobile (コンポーネント)     ← Phase 4で分離
7. enemy-bullet (コンポーネント)     ← Phase 4で分離
8. enemy-turret (コンポーネント)     ← Phase 4で分離
9. enemy-drone-black (コンポーネント)← Phase 4で分離
10. weapon-controller (コンポーネント)← Phase 4で分離
11. player-arrow (コンポーネント)    ← Phase 4で分離
12. checkGameClear() (関数)        ← Phase 3でGameManagerに
13. スタート画面制御                ← Phase 3でUIManagerに
```

### 既存の優れた点（保持すべき資産）
- ✅ GLBロード機構（ModelManager）
- ✅ Blender座標系変換ロジック
- ✅ 3秒ディレイによる即死バグ対策
- ✅ Box3による高精度当たり判定
- ✅ 敵AIの挙動調整（カクカク移動・爆発タイミング等）
- ✅ ゲームバランス（敵速度・弾速・ダメージ）

---

## 🗺️ 最終目標のディレクトリ構造

```
snipe-and-slash/
├── public/
│   ├── models/
│   │   ├── drone_white.glb
│   │   ├── drone_black.glb
│   │   └── sword.glb
│   └── index.html
├── src/
│   ├── domain/                 # ビジネスロジック（Pure関数）
│   │   ├── gameState.ts       # ゲーム状態定義
│   │   ├── gameRules.ts       # ゲームルール
│   │   └── types.ts           # 型定義
│   ├── managers/              # 状態管理・外部連携
│   │   ├── GameStateManager.ts
│   │   ├── ModelManager.ts
│   │   └── UIManager.ts
│   ├── components/            # A-Frameコンポーネント
│   │   ├── weapons/
│   │   │   ├── SwordComponent.ts
│   │   │   ├── ProjectileComponent.ts
│   │   │   └── WeaponController.ts
│   │   └── enemies/
│   │       ├── EnemyMobile.ts
│   │       ├── EnemyTurret.ts
│   │       ├── EnemyDroneBlack.ts
│   │       └── EnemyBullet.ts
│   ├── utils/                 # ユーティリティ
│   │   └── aframeHelpers.ts
│   └── main.ts                # エントリーポイント
├── tsconfig.json
├── package.json
├── vite.config.ts             # Viteでビルド
└── netlify.toml
```

---

## 📅 Phase別実施計画

### Phase 0: 準備（1-2時間）✅ 今ここ

**タスク**:
- [x] 現状分析
- [ ] この計画書作成
- [ ] Gitブランチ作成
- [ ] バックアップ

**成果物**:
- `REFACTORING_PLAN.md`（このファイル）
- Gitブランチ: `refactor/typescript-migration`

**確認方法**:
```bash
git checkout -b refactor/typescript-migration
git add REFACTORING_PLAN.md
git commit -m "docs: リファクタリング計画書を作成"
```

---

### Phase 1: TypeScript化（3-4時間）

**目標**: 既存コードを.tsに変換、型定義を追加（ロジック変更なし）

#### 1.1 環境構築
- [ ] package.json作成
- [ ] TypeScript・Vite・A-Frame型定義インストール
- [ ] tsconfig.json設定
- [ ] vite.config.ts設定

#### 1.2 型定義作成
- [ ] `src/domain/types.ts` 作成
  - GameState型
  - ModelManager型
  - 各コンポーネントの型

#### 1.3 game.js → game.ts変換
- [ ] game.js → src/main.ts にリネーム
- [ ] 型注釈追加（any禁止、unknown許容）
- [ ] ビルド確認

**確認方法**:
```bash
npm run dev  # Viteでホットリロード確認
# ブラウザで動作確認（挙動が完全に同じか）
```

**コミット**:
```bash
git add .
git commit -m "feat: TypeScript環境構築"
git commit -m "feat: 型定義追加（動作変更なし）"
```

**成果物**:
- `src/main.ts` (game.jsと同等の動作)
- `src/domain/types.ts`
- `tsconfig.json`

---

### Phase 2: ファイル分割（4-5時間）

**目標**: main.tsを機能ごとに分割（ロジック変更なし、コピペ移動のみ）

#### 2.1 domain層の抽出
- [ ] `src/domain/gameState.ts`
  ```typescript
  // GameStateの定義をコピペ
  export const GameState = { ... };
  ```

- [ ] `src/domain/types.ts`（既存に追加）
  ```typescript
  // 各種型定義を集約
  export interface GameStateType { ... }
  ```

#### 2.2 managers層の抽出
- [ ] `src/managers/ModelManager.ts`
  ```typescript
  // ModelManagerをコピペしてクラス化
  export class ModelManager { ... }
  ```

- [ ] `src/utils/hudHelpers.ts`
  ```typescript
  // updateHUD()をコピペ
  export function updateHUD() { ... }
  ```

#### 2.3 components層の抽出（A-Frame用）
- [ ] `src/components/weapons/SwordComponent.ts`
  ```typescript
  // AFRAME.registerComponent('sword', ...)をコピペ
  export function registerSwordComponent() {
    AFRAME.registerComponent('sword', { ... });
  }
  ```

- [ ] 同様に全コンポーネントを分割

**確認方法**:
```bash
npm run dev
# 全ての武器・敵が正常動作するか確認
# HUD更新確認
# クリア画面確認
```

**コミット**（1ファイルごと）:
```bash
git add src/domain/gameState.ts
git commit -m "refactor: GameStateをdomain層に抽出"
# （以下同様、ファイルごとにコミット）
```

**成果物**:
- `src/domain/gameState.ts`
- `src/managers/ModelManager.ts`
- `src/components/weapons/*.ts`
- `src/components/enemies/*.ts`

---

### Phase 3: クラス化・依存性注入（5-6時間）

**目標**: グローバル変数削除、クラスベース設計に移行

#### 3.1 GameStateManagerクラス化
- [ ] `src/managers/GameStateManager.ts`
  ```typescript
  export class GameStateManager extends EventEmitter {
    private state: GameStateType;
    
    incrementKills() {
      this.state.kills++;
      this.emit('killsChanged', this.state.kills);
    }
  }
  ```

- [ ] グローバルGameStateを削除
- [ ] 全コンポーネントでGameStateManager利用

#### 3.2 ModelManagerクラス化
- [ ] Promiseベース → async/await
- [ ] シングルトンパターン実装

#### 3.3 UIManagerクラス作成
- [ ] HUD管理
- [ ] スタート画面管理
- [ ] クリア画面管理

**確認方法**:
```bash
npm run dev
# 状態管理が正常に動作するか
# HUDがリアルタイム更新されるか
```

**コミット**:
```bash
git commit -m "refactor: GameStateManagerクラス化"
git commit -m "refactor: ModelManagerクラス化"
git commit -m "feat: UIManager実装"
```

**成果物**:
- `src/managers/GameStateManager.ts`
- `src/managers/UIManager.ts`
- グローバル変数ゼロ化

---

### Phase 4: コンポーネント設計改善（6-7時間）

**目標**: A-Frameコンポーネントを疎結合化

#### 4.1 武器システム抽象化
- [ ] `src/components/weapons/WeaponBase.ts`（抽象クラス）
- [ ] SwordComponent・BowComponent（具象クラス）
- [ ] WeaponFactory実装

#### 4.2 敵システム抽象化
- [ ] `src/components/enemies/EnemyBase.ts`（抽象クラス）
- [ ] 各敵クラス継承

#### 4.3 依存性注入
- [ ] コンポーネント初期化時にManager注入
  ```typescript
  AFRAME.registerComponent('sword', {
    init() {
      this.gameState = container.resolve(GameStateManager);
      this.modelManager = container.resolve(ModelManager);
    }
  });
  ```

**確認方法**:
```bash
npm run dev
# 全機能が動作するか総合テスト
# 新武器追加のテスト（仮実装）
```

**コミット**:
```bash
git commit -m "refactor: 武器システム抽象化"
git commit -m "refactor: 敵システム抽象化"
git commit -m "feat: 依存性注入実装"
```

**成果物**:
- `src/components/weapons/WeaponBase.ts`
- `src/components/enemies/EnemyBase.ts`
- DIコンテナ実装

---

### Phase 5: 最適化・ドキュメント（2-3時間）

**目標**: パフォーマンス最適化・ドキュメント整備

#### 5.1 パフォーマンス
- [ ] バンドルサイズ確認
- [ ] 不要なimport削除
- [ ] Tree-shakingの確認

#### 5.2 ドキュメント
- [ ] README.md更新（開発環境・ビルド方法）
- [ ] 各クラスのJSDoc追加
- [ ] アーキテクチャ図作成

#### 5.3 最終確認
- [ ] Quest 2/3実機テスト
- [ ] Netlifyビルド確認
- [ ] デプロイテスト

**コミット**:
```bash
git commit -m "docs: README更新"
git commit -m "docs: JSDoc追加"
git commit -m "perf: バンドルサイズ最適化"
```

**成果物**:
- 更新されたREADME.md
- アーキテクチャドキュメント

---

## ⚠️ リスク管理

### バックアップ戦略
1. **Gitタグ**: 各Phase開始前に`phase-N-start`タグ作成
2. **動作確認**: 各Phase終了時に必ず実機テスト
3. **ロールバック**: 問題発生時は前Phaseに戻す

### 緊急時対応
```bash
# Phase 2で問題発生時
git reset --hard phase-1-complete
```

---

## 📊 進捗管理

| Phase | 状態 | 開始日 | 完了日 | 備考 |
|-------|------|--------|--------|------|
| Phase 0 | ✅ 完了 | 2026-02-10 | 2026-02-10 | 計画書作成完了 |
| Phase 1 | ✅ 完了 | 2026-02-10 | 2026-02-10 | TypeScript環境構築完了 |
| Phase 2 | ✅ 完了 | 2026-02-10 | 2026-02-10 | ファイル分割完了（468行の新規コード） |
| Phase 3 | ⏳ 未着手 | - | - | - |
| Phase 4 | ⏳ 未着手 | - | - | - |
| Phase 5 | ⏳ 未着手 | - | - | - |

---

## 🎯 完了条件

### Phase完了チェックリスト
- [ ] ビルドが通る（TypeScriptエラーゼロ）
- [ ] ゲームが正常動作（PC＋Quest実機）
- [ ] 既存機能が100%動作
- [ ] Git履歴が適切
- [ ] ドキュメント更新

### 最終完了条件
- [ ] 全Phaseのチェックリスト完了
- [ ] パフォーマンス劣化なし
- [ ] Netlifyデプロイ成功
- [ ] 開発日記.md更新

---

**最終更新**: 2026-02-10  
**次回アクション**: Phase 0完了 → Phase 1開始承認待ち
